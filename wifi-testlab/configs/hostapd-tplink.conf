# wifi-testlab — visible AP: TP-LINK_8907_5G
# The 'interface' line is overridden by setup.sh at runtime.
interface=wlan0
driver=nl80211

ssid=TP-LINK_8907_5G
hw_mode=g
channel=11

wpa=2
wpa_passphrase=veilbreak-test-only
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP

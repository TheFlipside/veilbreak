# wifi-testlab — visible AP: DDW36563
# The 'interface' line is overridden by setup.sh at runtime.
interface=wlan0
driver=nl80211

ssid=DDW36563
hw_mode=g
channel=1

wpa=2
wpa_passphrase=veilbreak-test-only
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP

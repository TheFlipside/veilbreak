# wifi-testlab — client connecting to hidden AP
# scan_ssid=1 enables active probing (required for hidden SSIDs).
ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=0

network={
    ssid="VeilbreakLab"
    scan_ssid=1
    psk="veilbreak-test-only"
    key_mgmt=WPA-PSK
}

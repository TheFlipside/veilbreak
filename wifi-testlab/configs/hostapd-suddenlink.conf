# wifi-testlab — visible AP: SuddenLink990
# The 'interface' line is overridden by setup.sh at runtime.
interface=wlan0
driver=nl80211

ssid=SuddenLink990
hw_mode=g
channel=6

wpa=2
wpa_passphrase=veilbreak-test-only
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
